# Visualization

The current renderer exports standalone HTML with:

- cart, rail, N poles, joints, tip trail, and force arrow
- step, return, risk, and urgency overlay
- graph nodes colored by state
- edge weights, fast deltas, and bandit choices in trace data

This avoids a build system and keeps reports easy to archive.

